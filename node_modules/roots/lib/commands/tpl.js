// Generated by CoffeeScript 1.7.0
(function() {
  var Roots;

  require('colors');

  Roots = require('../');

  exports.execute = function(args) {
    var promise;
    args = args._;
    if (args.length < 2) {
      return console.log('help');
    }
    promise = (function() {
      switch (args[1]) {
        case 'add':
          return Roots.template.add({
            name: args[2],
            url: args[3]
          });
        case 'remove':
          return Roots.template.remove(args[2]);
        case 'list':
          return console.log(Roots.template.list({
            pretty: true
          }));
        case 'default':
          return Roots.template["default"](args[2]);
        case 'reset':
          return Roots.template.reset();
      }
    })();
    if (promise) {
      return promise.done(function(r) {
        return console.log(r.green);
      }, function(e) {
        return console.error(("" + e).red);
      });
    }
  };

}).call(this);
